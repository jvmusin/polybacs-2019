Конвертер архивов из формата полигона в формат бакса.

Как использовать конвертер:

Сначала надо его собрать. Для этого есть два способа.

Способ первый. Простой:
Зайти на https://github.com/PloadyFree/polybacs/releases и скачать оттуда jar-файл.

Способ второй. Посложнее
Скачать весь проект.
Затем из консоли зайти в папку с проектом и собрать его командой "./mvnw package" (без кавычек).
Затем взять получившийся jar-ник из target/musin.polybacs-1.0-SNAPSHOT.jar и (желательно) переименовать его во что-то попроще. Например, в "qq.jar".

Далее об использовании:
Берём этот jar-ник и кладём в какую-нибудь папку, рядом с ним кладём нужные ЛИНУКСОВЫЕ zip-архивы полигона.
Рядом с этими архивами кладём нужные pdf-условия.
В линуксовых архивах условия кривые, поэтому надо скачать условие отдельно и назвать его так же, как и архив.
Так, например, если имя архива было my-first-problem-5$linux.zip, то имя пдфки должно быть my-first-problem-5$linux.pdf
Если рядом не положить пдфку, то её не будет в архиве, но её можно будет добавить отдельно.
В консоли переходим в ту папку, куда положили jar-ник, и набираем "java -jar qq.jar" (без кавычек).
Если хотим, чтобы к каждой задаче добавился некоторый префикс, то набираем "java -jar qq.jar my-prefix". Тогда имя задачи при конвертировании будет добито указанным префиксом.
В случае успеха появится папка ready с готовыми архивами.

Если в какой-то из моментов консоль сказала, что она не понимает, что такое java или javac, значит в PATH не прописан путь до jdk.
Пропишите его (вплоть до папки bin, то есть, например, C:\Program Files\Java\jdk1.8.0_192\bin) и перезапустите консоль.
Теперь всё должно заработать.


Если в задаче требуются файлы, то они пока не поддерживаются. Может, когда-нибудь потом.
Если хочется прописать своё имя в авторах задачи, делайте ручками. Может, когда-нибудь потом сделаю.
